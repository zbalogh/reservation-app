1. Run the following command to get services and LoadBalancerIP:

kubectl get services

kubectl get all

kubectl get services -n istio-system

Look for the "istio-ingressgateway" LoadBalancer service. You will find the External IP address.


2. To access aplication open Web browser with the IP:

http://<EXTERNAL-IP>/web-gui/angular-gui/
